# MED-SECURITY
This project enforces continuous Zero-Trust access control, where digital sessions remain active only while the user is actively verified. Instead of relying on static logins or manual logout, the system automatically locks access when trust conditions fail, preventing unauthorized data exposure
